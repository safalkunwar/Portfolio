// Portfolio constants
// Note: OAuth/Manus integration has been removed.
// These exports are kept for backwards compatibility if any shared code depends on them.

export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";
